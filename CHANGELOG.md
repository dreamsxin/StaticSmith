# 变更日志

记录**用户能感知的变化**：新功能、行为改变、修掉的毛病。纯内部重构与文档措辞不进这里。

版本号写在 `Cargo.toml`（workspace）与 `package.json` 两处，发版时一起改。
仓库此前没有打过 tag，所以 2.0.0 之前的条目不做追溯——那段历史请查 `git log`。

## 未发布

### 新增

- **写操作前自动留本地快照，可一键回退**。批量替换、批量搬动、删除内容一直都有干跑
  预览，但落盘之后没有撤销——而 MCP 把这些写工具开给了 Agent，干跑只在人真的逐条读了
  预览时才起作用。现在 MCP 的写工具在动手之前会自动存一份快照，提交信息就是工具名；
  `list_snapshots` 看历史，`restore_snapshot` 退回去。回退**前**的状态也会先存一份，
  所以回退错了还能再回来。
  快照存在 `.staticsmith/history.git`，是一个独立于你自己 git 仓库的影子仓库：
  工作树是项目根，但仓库目录在 `.staticsmith` 里，项目根**不会**出现 `.git`，
  你的 `git log`、暂存区、`git status` 都不受影响。跟踪范围按配置里的实际目录算，
  只含源（配置、内容、模板、当前主题、静态资源），产物目录不进快照。
  你自己的 `.gitignore` / `.gitattributes` 不会影响快照内容，回退是字节级还原。
  只生成产物的操作与干跑不留快照。
  桌面端共用同一条历史：界面上的删除、批量动作、栏目改名与删除、跨文件替换在动手前
  同样会留快照。仍未覆盖的是单文件保存、新建、导入、主题装入与配置保存。
  界面上暂时还没有浏览 / 回退快照的入口，回退要走 MCP 或直接用 git。



- **新建站点可选版式**：起始页与 `staticsmith init --preset docs|blog` 都能选
  「橙色极简文档站」或「博客园风格博客」。菜单「站点 → 新建站点…」会弹一层表单，
  字段与起始页那张卡片一模一样（同一个组件，加字段只改一处）——版式推导不出来，
  事后换代价也不小（一套是十来个模板文件），所以两个入口都让你先选。
  两套写出的文件路径完全一致，只换模板与样式，所以换版式不影响任何既有的内容路径。



- **新建内容可以直接写路径**：`posts/2026/09/hello.md` 这种归档式目录推导不出来，
  只能手写。填了路径就以它为准，栏目输入框随之禁用——两个都参与推导会出现
  「栏目填 posts、路径填 notes/x.md」这种自相矛盾的输入。省略后缀会自动补 `.md`。

- **模板上下文补三个全站变量**：`terms`（词条及篇数，以前只在标签页有，所以全局侧栏
  做不出标签云）、`terms_by_field`（多维度站点按字段名取）、`feed_url` / `sitemap_url`
  （以前模板里只能手写 `/feed.xml`，与生成侧各写一遍迟早对不上；开关关掉或
  `base_url` 为空时是 `null`）。博客版式的侧栏据此换成了真标签云。

- **新建栏目可以同时填简介**：以前建完立刻就会被 SEO 体检记一条「缺描述」，
  等于每次建栏目都先欠一笔账。


- **站点可选正文格式**：「设置 → 构建 → 正文格式」在 Markdown 与 HTML 之间选，
  对应 `[build] source_format`。选 HTML 就是**原样输出**：写什么标签就是什么标签，
  `**` 只是两个星号。全站统一、不支持每篇覆盖，也不做「HTML 里仍跑一遍 Markdown」的
  混合模式（那种模式下「这段为什么被转义了」永远解释不清）。`content/` 下的
  `.html` / `.htm` 文件两种模式都会被收进来，切换格式不必改文件名。
  编辑器的高亮也跟着格式走：HTML 站点按标签、属性、注释着色，不再把满屏标签当普通文字。

- **设置页新增「源码」分段**：直接编辑 `staticsmith.toml`，逐字写回——不格式化、
  不重排键。表单认不出的键、写在配置里的注释、给别的工具看的段，现在都有地方改。
  写盘前先解析并校验，解析不了就不落盘（否则下一次打开站点连设置页都进不去）。
  它有自己的保存按钮：公共的「保存设置」写的是表单折算出的配置，会盖掉手写文本。

- **界面模式**：「视图」菜单里一键切换整套布局——标准、专注写作（只留编辑器与工具条）、
  源码（列表 + 编辑器，front matter 直接在 `+++` 里改）。类比 VS Code 的布局预设，
  记在本机，当前模式显示在状态栏右侧。模式只改布局不改能力：菜单、快捷键、
  命令面板在任何模式下都是全的；切过去之后每个窗格仍可单独开关。
  「视图」菜单同时多了「属性面板」「格式工具条」两个开关。
  定位也随之写明：正文就是源码，不做块级富文本（README 与 docs/ui.md）。

- **单篇内查找替换**：编辑器里 `Ctrl+F` 展开查找／替换那一行（上一处、下一处、
  替换这处、全部替换、忽略大小写，带「第 3 / 12 处」计数）。找**文章**改用
  `Ctrl+Shift+F`——`Ctrl+F` 现在总是「在你正看着的东西里找」。
  替换只改编辑器里的内容，落盘仍走 `Ctrl+S`。

- **跨文件查找替换**：「编辑 → 跨文件替换…」在侧栏展开，或命令行
  `staticsmith replace --find 旧名 --to 新名`，AI 助手侧是 `replace_text` 工具。
  改一个称呼、统一一个术语不必再逐篇点开。
  **只改正文，front matter 一个字节都不碰**（要改标题、标签用批量动作或属性面板），
  纯文本不支持正则，且**必须先看干跑结果**——哪几篇、共几处、每处前后对照，
  正文替换没有撤销栈。命令行默认只干跑（加 `--yes` 落盘），MCP 默认 `dry_run`。

- **侧栏能搜正文**：搜索框给两档结果——「标题或路径里有这个词」（本地即时过滤）与
  「正文里提到」（带一段上下文片段）。判断与 MCP 的 `search_content` 共用一份实现，
  界面搜到的与 AI 助手搜到的是同一批。
- **设置页新增「AI 接入」**：在界面里就能开 MCP 端点（只监听 127.0.0.1），
  不必再开终端跑 `staticsmith mcp`。权限默认只读，「改内容与模板」「执行发布」
  要显式勾选；权限只在启动时决定，界面显示的权限从正在跑的服务端读回来。
- **编辑区空态给出可点的三步**：写第一篇 → 生成一次 → 发布出去，完成态取真实状态，
  写过且生成过之后自动消失。
- **右键菜单**：内容条目与栏目分组头都能右键，与行内的「⋯」按钮共用同一份动作
  （打开、预览、发布/设为草稿、复制路径、删除；栏目还有新建、信息、改名、删除空栏目）。
- **日历能改排期**：「接下来」每行有「改期…」，「没写日期」每行有「补日期…」，
  就地改完即写盘。这一页从「看节奏」变成「排节奏」。
- **`staticsmith batch`**：加去标签、切草稿、搬栏目、删除四件事补进命令行，
  脚本化整理站点不必再回界面。搬动默认补旧地址，删除默认只干跑（真删要 `--yes`），
  判断与桌面端、MCP 共用 `staticsmith_core::batch`。
- 新增面向写作者的 [上手教程](docs/getting-started.md)：不假设读者懂 Markdown、
  Git 或命令行，走完一遍就有一个能发出去的站点。

### 变化

- 内存预览现在**能看到图片与样式**了：本地样式表、图片、字体（含 CSS 里的 `url()`）
  会嵌进预览 HTML——`srcdoc` 沙箱没有文件访问权限，只能这么办。单个超过 2 MB
  或一页累计超过 8 MB 的会被放弃，预览栏底部写明还有几处没嵌进来。
  脚本与外链资源仍不加载，「与线上完全一致」仍然只有本地服务器模式能保证。
- 从未生成过时启动本地预览服务器，会先自动生成一次。服务器读 `dist/`，那目录还空着
  的时候点「启动」只换来白屏，用户会以为服务器起不来。只在从未生成过时补这一步——
  有旧产物至少能看，而每次启动都强制重建只会让人等。

- `Ctrl+S` 与 `Ctrl+F` 提升为全局快捷键：焦点在属性面板、侧栏、预览栏里也能保存；
  `Ctrl+F` 会自己切到「内容」页、必要时打开被关掉的列表栏再聚焦。
- `Ctrl+S` **按上下文分派**：外观页看着模板时保存模板，其余保存文章；菜单项标题
  跟着变（「保存当前模板」／「保存当前文章」）。补上了文档早就承诺、代码却没实现的
  「模板保存快捷键」。
- `Ctrl+P` 从开关改成「打开并聚焦」，与标识行按钮、视图菜单三处语义一致，关闭统一用 `Esc`。
- 生成警告合并成一条通知，不再逐条把通知区刷满一屏。
- 「重新读取磁盘上的内容与模板」成功后有反馈了（以前点了什么都不显示）。
- 窗口宽度不足时自动收起预览栏（< 1200px）与列表栏（< 900px）；手动动过的那一栏
  不再自动收放。窗口缩小时重新夹取分栏宽度，预览栏不会再把编辑器挤成一条缝。
- 内容筛选从五个药丸按钮改成一行下拉，选项文字把作用写全
  （「草稿（还没发布）」「待生成（改过还没生成）」这样）。
- 栏目列表页（`posts/index.md` 这类）在内容列表里带「栏目页」徽标，并在日历的
  「没写日期」区块说明它不参与排期——它此前跟文章长得一样，于是「日历上少了一篇」。
- 产物类型在两处叫同一个名字：「生成」页的产物清单与侧栏的「站点页面（生成）」
  统一说「标签页 / 分页页」。侧栏原先说「标签 / 分页」，还会把站点地图、订阅源
  一律标成「分页」。
- 主结构现在能只用键盘走：标签栏用 `←` `→` 换视图，分栏拖拽条能被 Tab 走到、
  方向键调宽（`Shift` 加速、`Home` / `End` 到两端），月历用方向键走格子、
  `PageUp` / `PageDown` 翻月、回车进入那天的条目、`Esc` 退回。
  一片结构只占一个 Tab 停靠点，不会为了跳过日历按四十多下 Tab。
- 按钮、输入框与分栏拖拽条的边界加深到能过 3:1（新增 `--border-strong` 令牌）。
  原来那档 `--border` 对底只有 1.45:1，而按钮底色与内容底只差 1.07:1——
  「这里能点」这件事此前几乎全靠形状猜。纯装饰的分隔线仍用原来的浅色，
  整套两套主题的令牌对比度核查结果写进了 docs/ui-design.md 6.11。
- 深色模式补齐令牌：主色与警告色有了深色值（砖红提亮且保持色相），
  新增成功色与「实心色块上的文字色」，界面里不再有绕过令牌的字面色值。

### 修复

- **命令内部出错不再带走整个窗口**。release 构建以前设了 `panic = "abort"`，
  任何一处程序缺陷都会让窗口当场消失、未保存的编辑一起丢。现在改成在 IPC 边界上兜住：
  你会看到一条错误信息，编辑器还活着，磁盘上的内容没受影响。写入路径捕获后会立刻从
  磁盘重载状态——出错可能停在改了一半的地方，拿着半套内存状态继续生成产物比报错更糟。
- `site_info` 与日志里的路径不再出现 `...\站点\.\content` 这种夹着 `.` 的写法。

- **保存设置不再抹掉配置文件里的注释**。以前「设置」页每保存一次就把
  `staticsmith.toml` 整文件重新序列化，你写的 `# 这台机器上别开 minify` 之类的说明、
  以及 StaticSmith 不认识的段（给别的工具看的那些）全部消失。现在走保序改写，
  与 front matter 同一套办法。唯一例外是数组表（`[[taxonomies]]`、`[[menu]]`）整块替换，
  它们内部的注释仍会丢——已写进 docs/configuration.md。
- 发布页的「凭据条目」名改由 Rust 侧算出（新增 `deploy_account` 命令）。
  界面此前自己拼 `git:<remote>`，与 Rust 侧各写一份：改了规则就会「保存写进 A、
  查询读的是 B」，用户看到的是「明明保存过，界面说没有」。
  顺带核实并写清了一件事——凭据按**发布目标**分而不按站点分，两个站点推同一个远端
  共用一条凭据是刻意的；代价是同一目标存两套凭据时后保存的会覆盖前一条。
- 生成结束后状态栏回不到「就绪」（进度文字停在最后一条）。
- 忙态在一次操作里反复跳变，按钮闪烁、`disabled` 提前解除：改成计数器，
  复合动作（保存、生成、批量、打开站点）整段圈成一次忙态。
- 空列表看起来像坏了：外观页四个列表、构建历史、发布结果、「站点页面（生成）」
  各自给出「接下来做什么」，不再整块消失或只剩一个标题。
- 键盘与读屏用户的一批问题：命令面板补 `aria-modal` 与焦点收束、浮层与菜单关闭后
  归还焦点、菜单标题上能用方向键、就地删除确认默认聚焦「取消」、
  改名与「栏目信息」表单展开即聚焦输入框。
- 分栏宽度每次变化都同步写一次 localStorage，拖动手感发涩：改成 200ms 防抖。
- MCP 的 `search_content` 搜的是渲染后的 HTML（搜 `img` 会命中每一张带图的文章），
  片段还丢掉了原文大小写。现在先还原成纯文本再搜，片段保留原文。
- 对齐 Tauri 的 npm 包与 Rust crate 版本，修掉 `npm run tauri dev` 起不来的根因。

## 2.0.0

首个记录在案的版本，能力范围见 [README](README.md#能力现状)。要点：

- 桌面端（Tauri 2 + Vue 3）：内容编辑与实时预览、栏目与批量动作、发布节奏日历、
  外观与主题包、三种体检、生成与发布、命令面板。
- 生成引擎（`staticsmith-core`）：Markdown + TOML front matter、Tera 模板与依赖图驱动的
  增量构建、SQLite 索引、内容寻址的媒体资源、taxonomy 聚合、sitemap 与 Atom 订阅、
  定时发布、旧地址重定向。
- 命令行（`staticsmith`）：`init` / `new` / `build` / `plan` / `serve` / `deploy` /
  `check` / `audit` / `import` / `theme` / `mcp`，`audit --fail-on` 可当 CI 门禁。
- MCP 服务端：三级权限（默认只读），供 AI 助手读写站点。
- 发布：Git 与 FTP/SFTP 差异同步，凭据存操作系统凭据管理器。
