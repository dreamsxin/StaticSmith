# 变更日志

记录**用户能感知的变化**：新功能、行为改变、修掉的毛病。纯内部重构与文档措辞不进这里。

版本号写在 `Cargo.toml`（workspace）与 `package.json` 两处，发版时一起改。
仓库此前没有打过 tag，所以 2.0.0 之前的条目不做追溯——那段历史请查 `git log`。

## 未发布

### 新增

- **编辑器页脚多了「← 上一篇 / 下一篇 →」**。写书是顺着往下写的：写完这一章接着写
  下一章，原先每次都要回侧栏找。顺序就是目录的顺序，也就是**读者在网站上看到的顺序**，
  不跟侧栏的搜索与筛选走（否则搜过一次之后「下一篇」会突然跳过好几章）。
  到头置灰而不是藏起来，也不会转一圈回到第一篇。打开走的是与侧栏点一行完全相同的路，
  所以未保存的改动照样会先问一句。


- **拖动文章时列表会跟着滚**。目录一长，把一篇拖到屏幕外的栏目上原先根本做不到：
  手一直按着，列表不动，只能松手、滚、再拖一次。现在指针贴到列表上下边缘就自动滚，
  越靠边越快但有上限；拖到列表外面也不再加速——那会让「拖过头」变成「一路窜到底」。


- **侧栏里能把一篇文章拖到另一个栏目**，像在书的目录里把一章挪到另一部分。
  原先跨栏目拖动是刻意不做的：它等于改归属，要补旧地址、还要改写别人文章里指向它的
  链接——那必须先确认。现在拖到别的栏目上，整个分组亮起「可以放这里」，松手**不直接落盘**，
  而是就地摆出「搬到哪、旧地址保留成什么、另会改写哪几篇里的多少处链接」，确认了才写。
  与批量条的「移动到栏目」走同一条路，所以两处行为不可能不一致。
  两种情况不接：栏目索引页（它的地址就是栏目名，要走「栏目改名」）、拖回自己所在的栏目
  （等于没动）。**跨栏目时不接「插到某一行前后」**——那一次拖拽同时改了归属和顺序，
  一句话说不清会发生什么；顺序是搬完之后的第二步。


- **有了一张快捷键一览**（`Ctrl+P` → 「快捷键一览…」，帮助菜单里也有），
  按**作用域**分三组：随处可用 / 在编辑器里 / 在浮层与列表里。分组是它的全部价值——
  「在哪儿按才有用」是最常猜错的一件事（`Ctrl+B` 在列表里按下去毫无反应，
  不是坏了，是它属于编辑器）。原先界面上唯一的一览是编辑器页脚那六个，
  而且只在打开文章后才看得到，十几个键位里能被发现的不到一半。
  页脚那一行现在与这张表同一个来源，不会再出现「页脚说 `Ctrl+K` 是链接、一览忘了列」。

- **样式表有了形状、阴影、层级三套令牌**，界面里不再出现裸值（测试拦着）。
  这是「先把地基砌齐」而不是一处处补：
  - **圆角三档**（小片 4px / 控件 6px / 容器与浮层 10px，外加只读标注的药丸）。
    原先散着 3 / 4 / 6 / 8 / 10 / 999px 六种，没人说得清哪种归哪类；而规范里写的
    「只有一个值」也是错的——控件与容器必须分开，否则「能点的」和「装东西的」长得一样。
    几处容器的圆角因此从 8px 变成 10px。
  - **阴影三档，且深色模式单独给一套更浓的**。原先三处各自硬编码 18–25% 的黑，
    在近黑背景上几乎看不见——浮层只剩 1px 描边，看着像贴在页面上而不是浮起来。
  - **层级五档，每档写明压住谁**（钉住的工具条 → 列表头 → 下拉与命令面板 →
    右键菜单 → 通知）。原先是五个裸数字散在五处，「谁压谁」只能靠通读全文去记，
    加一层新浮层时只能猜。中间留了空档，插新层不必给所有人挪位置。


- **根目录多了 `AGENTS.md`**：给要改这个仓库的 AI Agent 的一页说明书——与 CI 等价的
  验证套件、改动的同步点清单（漏一处就是「功能看起来做了却调不到」）、十条硬规矩
  及其来历、以及会浪费半小时的已知坑。它不重复 `docs/development.md`，两份交叉引用。


- **忙的时候窗口顶上会有一条扫动的细线**。以前只有按钮置灰加状态栏一行小字，而全站生成、
  跨文件替换要跑几秒到几十秒，界面其余部分看起来完全正常——用户会以为「刚才那一下没点中」。
  不做遮罩（这些操作并不禁止阅读），关掉动效的人看到的是一条静止的浅色带。
- **「待重新生成」换成 `↻`**，不再和「未保存」共用一个 `●`：两者共用时，保存完那个圆点
  还亮着，用户只会得出「保存没生效」。


- **目录里能把一整节挪上挪下了**。挪的是「标题 + 它下属的全部内容」（子标题与正文一起走），
  这才是纸质书里「调整章节顺序」的意思——挪标题不挪正文只会把文章拆散。
  悬停某条标题会显出 ↑ ↓ 两个按钮，到头到尾自动置灰。
  **不跨父标题**：「把 2.1 挪到第 1 章下面」改变的是它属于谁，那是搬动而不是排序。
  两节之间的空行与文件末尾的换行都留在原地，所以挪过去再挪回来一字不差。
  改动只落在编辑器缓冲区里（与手打一样），保存之前随时可以不存。


- **侧栏现在既是整本书的目录，也是这一章的细目**。正在编辑的那一篇会把自己的篇内标题
  摊在它那一行下面，点一条就跳到那一节。这样「栏目 → 文章 → 小节」在同一列里连成一条线
  ——这是「编辑网站像编辑一本书」里最后一块没有落地的。
  只展开手上这一篇是刻意的：其余的要读盘才知道标题，一本两百章的书为了画目录读两百个文件
  不划算；而手上这一篇的正文已经在内存里，所以目录**边写边长**，连保存都不用等。
  大纲浮层（`Ctrl+Shift+O`）仍然留着，那是「用一下就走」的跳转器，两者的标题都出自同一处。


- **文章可以直接拖着排序了**（同一栏目内）。「上移一位」挪一格够用，把第 12 章拖到
  第 2 章要点十一次。整行都能抓，落点按中线画在上边或下边，所以「放到最后一位」也做得到；
  被拖的那行压暗，落点画一条线。拖回原处不写盘。
  **跨栏目拖不做**：那等于改章节归属，要补旧地址、改写站内引用，得先干跑再确认
  （批量条里的「移动到栏目」才是那条路）——所以拖到别的栏目上不给落点提示，
  松手也什么都不做。不做，比做一半更好解释。


- **栏目自己也能「上移一位 / 下移一位」了**。上一版给了文章，栏目却还要在「栏目信息」里
  手填一个「排序」数字——同一件事两种改法，而其中一种是把实现细节摆给用户看。
  现在两者手感一致：菜单里挪一位，到头到尾置灰，界面上再也见不到那个数字。
  第一次挪动会把整层的顺序固化下来；缺列表页的栏目会顺手补一张（位次只能存在那里，
  不补的话排完它还在原地），补了几张会在通知里说出来并列出文件名。


- **侧栏的栏目排成一棵树了**。子栏目现在紧跟在父栏目下面并缩进一级
  （`posts` → `posts/2026`），子栏目只显示末段（完整路径进悬停提示），
  标题左侧挂一条竖线。以前所有栏目是平铺的：`posts` 与 `posts/2026` 两个分组并排，
  看着像一堆文件夹，而不像一本书的目录——而站点结构本来就是有层级的（栏目就是目录，
  可以任意层嵌套）。搜索或筛选把父栏目整组去掉时，子栏目会自动报回完整路径，
  否则孤零零一个「2026」看不出是谁的。


- **侧栏的文章顺序现在就是网站上的顺序，而且能改**。以前侧栏按**源文件名字母序**列
  （`a.md` 永远在 `b.md` 前面），网站上却按「排过的位次 → 日期倒序」——同一篇文章
  在界面上是第 3 个、在站点上是第 7 个。这让侧栏更像文件夹而不像一本书的目录。
  现在两边共用同一套顺序规则（`content::reading_order` 与它在 `src/grouping.ts` 里的
  孪生实现，两边各有测试钉住同样的例子）。
  顺序也终于能在界面上调：文章右键菜单里多了**「上移一位 / 下移一位」**，已经在头 / 尾时
  置灰。界面上不出现「排序权重」这种数字——读者看到的只有先后。第一次挪动会把整栏的
  当前顺序固化下来（因此可能改写这一栏的每一篇），通知里会说清「把这一栏 N 篇的顺序
  固化下来（改写 M 篇）」，之后每次只动两篇。固化过之后新建的文章会出现在最前面：
  新写的东西该看得见，挪一次就固化进去了。


- **发布可以中途停下来了**。发布期间「一键发布…」换成「停止发布」，停止在**当前这个
  文件传完之后**生效——上传逐文件进行，文件之间的边界本来就不是原子的，所以停在边界上
  不会留下半个文件。停完照常给一份报告：传完了几个、线上此刻是「一半新一半旧」、
  再发一次会跳过已传的那些（与三次重试失败那条话是同一件事的两种成因）。
  停止**不是失败**，但结果区会明确说「上次发布是被停止的，不是发完了」——两者的上传数
  可能都是 12，一个是全部、一个是一半。
  Git 那条路只在提交之前能停：提交之后剩下的只有推送，而推送是整个 ref 的原子更新，
  中间没有安全边界可停；停在提交之前则线上一个字节都没变。
  **干跑期间叫停是错误**：一份少了一半的清单在界面上和完整清单长得一模一样，
  用户会照着它做决定「只要传 3 个」。但干跑本身也要能停——FTP 要逐个查远端状态，
  上千个文件时够久了，停不下来那段时间界面上只有一个转圈。
  命令行的 Ctrl-C 仍是硬杀，暂时没变。


- **「回退内容」里现在看得到历史占了多少地方**。快照是这套东西里唯一**随使用无声长大**
  的：每次不可逆操作留一份，换一张大图就多存一份那张图的完整副本（静态资源也在跟踪
  范围里）。看不到就永远不会想起清理，直到某天发现项目目录多了几个 G。现在常驻一行
  「历史占用 X（N 份）」；超过 200 MB 才追加清理办法（`git gc`）并说清代价——
  删掉的历史无法找回。我们不代劳这一步：libgit2 没有 gc，而调 `git` 二进制要求用户
  机器上装了 git，说到底删历史该由人自己按下。
  顺带把三个面板里各写一份的字节格式化收进 `text.ts`：同一个数字三种说法会被当成三件事。


- **命令行的回退也先列清单**。`staticsmith history restore <id>` 不带 `--yes` 时不再只说

  「将把源文件回退到 X」，而是列出会动哪些文件（`~` 换回旧版、`-` 删掉、`+` 找回），
  与桌面端、MCP 同一份数据。`--json` 直接给结构化的 `RestorePreview`。
  同一个能力三个入口都给到，这件事才算做完。


- **回退快照之前先给你看一眼**。选中一条快照后会列出这次回退会**换回旧版、删掉、找回**

  哪几个文件（三类各几个先说一句汇总，清单最多列 8 行）。回退动的是整个内容目录、模板与
  `staticsmith.toml`，而它此前只有一句「这之后的改动会被撤销」——是最后一个没有清单的
  写操作，偏偏也是一次动得最多的那个。什么都不会改时确认按钮置灰。
  MCP 的 `restore_snapshot` **默认变成干跑**（同 `deploy_site`），要真回退得显式传
  `dry_run: false`——Agent 少写一个参数就把整站内容换掉，是这套工具里代价最大的手滑。
  「回退内容」对话框此前完全没有测试，这一轮补了 11 条。


- **体检会把读不出来的文件报出来**（`content.unreadable`，error 级）。以前体检拿到的

  页面清单里根本没有这几篇（解析不了就没有 Page），于是站里有一篇坏文件时，体检既不报它、
  也不知道它存在——一个诊断工具在遇到病人时装作无事发生。现在它排在清单最前，
  点一下就打开那个文件按源码改（以前点体检里那一条没有任何反应）。


- **读不出来的文件在内容列表里点得开了**。上一版把「打不开项目」改成了「打得开、少一篇」，

  但那一篇只在一条通知里露过一面——用户知道出了事，却只能去文件管理器里找。现在内容栏
  最上面多一组「读不出来」，列出每一篇与原因，点一下就当纯文本改（属性面板此时用不了，
  编辑区顶上有横幅说明为什么）。改好保存，它就回到自己的栏目里。


- **批量动作跳过的那几篇，现在查得到**。以前只报第一条原因加一句「另有 N 篇被跳过」：

  五篇里有两种原因时，第二种从没露过面，而用户会按看到的那一条去排查。现在一行里
  按种类说全（「跳过 3 篇：2 篇「front matter 读不出来」、1 篇「目标已存在」」），
  **被跳过的每一篇进「消息」里的折叠明细**。同样处理的还有三处：改地址时没改成的引用、
  生成警告、导入警告——它们以前都是「第一条 等」，剩下的读完就没了。
  批量动作没有撤销栈，这份清单是判断「要不要去 Git 里回滚」的全部依据。


- **改不到的相对链接现在会提前报出来**。搬动、改 slug、栏目改名的干跑里多一条：

  「另有 N 篇里的 M 处相对链接（`../a/` 这类）指向它，改写不到，需要手工改」。
  相对地址要按**引用方**所在目录解析，而改地址改的是被引用方，两者对不上，
  所以自动改写只能处理 `/posts/a/` 这种根绝对地址。以前这些链接只会在
  「体检」页的死链里出现——那是**改完之后**，而这一条让你在动手前就看见。
  命令行 `staticsmith slug --dry-run` 与 `batch move --dry-run` 以 `!` 开头列出同一份清单。


- **栏目改名也先给你看一眼**。改名前列出会搬几个文件、给几篇补旧地址、另会把哪几篇里的
  几处站内链接改到新地址，确认才动。三条「改地址」的路里它一次动得最多（整棵子树），
  却是最后一个补上干跑的——搬动、改 slug、删除、替换、发布都早有这一步。
  MCP `rename_section` 带 `dry_run: true` 拿同一份结果。


- **发布之前先给你看一眼**。「一键发布…」不再一点就走：先算出这次会传哪些文件、
  跳过几个、总共多少字节、目标是哪里，就地列出来确认才发。命令行是
  `staticsmith deploy --dry-run`；MCP `deploy_site` **默认变成干跑**，
  要真发得显式传 `dry_run: false`。
  发布本来是这个项目里唯一没有这一步的写操作——批量、替换、删除、改地址都要先看一眼
  「会改哪些」，而唯一影响**线上**的动作却是一点就走：别的动作改错了还能在本地改回来，
  这个改错了是别人看到的页面变了。
  预览里也说清干跑自己的代价：FTP 会连服务器逐个查大小与时间（只读），
  Git 会在本地 `dist/.git` 暂存一次才能算出差异（不提交、不推送），
  以及「远端多余的文件不会被删除」——否则「上传 3 个」看起来像线上只剩这 3 个。


- **改地址（slug）现在会自己善后**。属性面板多了「地址（slug）」一栏（命令行
  `staticsmith slug <源文件> --to <新地址>`，MCP `change_slug`）：改完自动把旧地址补进
  `aliases`，并把站内其它文章里指向它的链接改到新地址。以前改 slug 只能手改 front matter，
  两样善后都得靠人记得——不补旧地址外链当场 404，不改站内引用就留下一批死链，
  而它们要等「体检」页才浮出来。落盘前会**先就地问一句**：「地址将从 A 改为 B，
  旧地址会保留，另会改写 N 篇里的 M 处站内链接」——这一步要写你没有点名的文件，
  与搬动、删除同一条约定（命令行是 `--dry-run`）。
  索引页那一栏置灰（它的地址就是栏目名，改它用栏目改名）；
  这一篇有未保存改动时会先让你存盘，免得缓冲区把刚写好的 front matter 盖回去。
- **栏目改名也会改写站内引用**了。整栏目换名等于一批地址同时变，
  以前只补 `aliases`，站内那些链接仍指着旧栏目。

### 修复

- **改完导航菜单，页面上的导航还是旧的**。界面说「设置已保存」，配置文件里也确实改了，
  但点「生成」只重算了标签页——文章页、首页、栏目列表页的头部导航停在旧版本，
  除非手动来一次全量重建。根因是增量判定只看模板与内容，而菜单、站点标题、`[extra]`
  属于**配置**，不属于任何一篇文章。现在配置变了就整站重算。
  代价是改任何一项配置（哪怕只是发布凭证）都会触发一次全量；这是有意的：
  少算一次的表现是「线上导航停在旧版本」，而那种错看起来一切正常。

- **FTP / SFTP 的重试现在会先重连**。上传失败最常见的原因就是连接本身坏了——控制连接被
  服务器按空闲超时踢掉、NAT 会话过期、网络切换。而重试一直拿着同一条坏连接再试，
  三次注定三次都失败：那套重试「看着有、其实没有」，还白等两轮退避。现在每次重试之前
  先重建连接（`RemoteFs::reconnect`）。重连本身失败也不打断——接着用旧连接再试一次上传，
  让上传自己的错误去决定成败，因为用户要的答案是「上传为什么失败、传到哪儿了」，
  而不是「重连也失败了」。


- **MCP 的 HTTP 端点：一个请求出内部错误不再让客户端干等**。工具执行早就兜住了 panic，

  但请求分派那一层（路由判断、读请求体、拼 JSON-RPC、查会话表）没有：真出问题时
  那个请求永远不回话，客户端只能等到超时，日志里也没有线索，而端口还在监听、
  界面上还写着「运行中」。现在回 500 并说明「已兜住，端点仍在运行」；请求线程外面
  还有最后一道，管发响应与 SSE 长流那一段。这是这个端点最后一处没有兜底的地方
  ——其余入口（stdio 循环、工具调用、桌面端的会话、预览服务器、文件监听）早就各有一道。


- **两处「不说话」的地方现在说话了**。一、版式清单读不出来时只写了 `console.warn`：

  用户看到一个空的版式列表，那看起来像「这个版本没有版式」而不像「读取失败了」，
  而打包好的桌面应用没人开着控制台。现在报一条提示（不是错误——还能用默认版式建站）。
  二、FTP 干跑那句「远端多余的文件不会被删除」把话说大了：这一层是逐个查本地文件
  对应的远端状态（不解析 `LIST`），根本不知道远端还剩什么。现在改成
  「只查了本地这 N 个文件对应的远端状态，远端另外还有什么查不到也不会动」——
  说得比知道的多，是这套东西里最不该有的毛病。顺带删掉 `plan_sync` 里那个
  从没被传过 `true` 的 `delete_extra` 参数：它装作能做镜像式清理，而那条路根本不通。


- **一篇 front matter 坏掉的文件不再让整站编辑不了**。以前 `+++` 少写一个结束符，

  打开项目就直接失败——界面停在起始页，剩下几十篇一篇也编辑不了，而修它的工具
  正是打不开的这一个。现在项目照常打开，读不出来的那几篇按原因汇总报出来
  （明细在「消息」里，每一篇一行），它们不进内容清单；**生成仍然会被拦下**，
  并说清是哪几篇——产物里静默少一页比报错难查得多。
  批量动作、跨文件替换、改地址也不再被一篇坏文件拖住；栏目改名遇到子树里有坏文件时
  明确拒绝（那一篇会跟着目录搬走，而它的旧地址算不出来、补不上重定向）。
  批量动作对这种文件也不再报「找不到这篇内容」——它明明就在列表里点得到，
  那句话把人往「路径写错了」的方向带。


- **站里有一篇 front matter 坏掉时，全站替换与搬动不再整体失败**。跨文件替换选「全站」、

  或者搬动/改地址要算「哪几篇里的链接会跟着改」时，只要有一篇文件的 `+++` 没闭合，
  整个操作直接报一句「front matter 无效」——连干跑都做不了。而这些功能恰恰是用来批量
  修东西的，「站里有一篇手改坏了」正是要修的那种情况。现在其余的照做，坏的那篇按
  「跳过 / 改不到」单独报出来，一个字节都不动。


- **命令行与界面、MCP 现在说同一句话**。同一次批量动作，界面说「跳过 2 篇（目标已存在）」、
  命令行说「跳过 2 篇」再逐条列、MCP 只回一个 JSON 数组——用户得在三处各学一遍。
  汇总措辞收进 `staticsmith_core::skips`（前端 `src/skips.ts` 与它对齐，两边各有测试钉住），
  命令行的 `staticsmith batch` / `replace` / `import` 都改用它，MCP 的 `replace_text`
  多回一句 `skip_summary`。命令行「一篇都不用改」时也不再报「改了 0 篇」。


- **一篇都没改成时不再报「成功」**。批量动作在「没有一篇需要改」时弹的是绿勾加

  「已处理 0 篇」——那看起来像做完了，没人会去查为什么。现在这种情况说
  「没有需要改的，什么都没动」；一篇都没做成而有跳过的按错误报。


- **「新建内容」与「新建栏目」不再叠在一起**。展开「新建栏目」之后再点右上角「新建」，

  两张表单会同时留在侧栏里（两个「创建」按钮、同一份栏目候选绑两次），
  看不出哪张才是当下要填的。三块可展开的表单（新建内容、新建栏目、跨文件替换）
  现在共用一个状态，互斥不再靠代码里记得逐个关掉。
  顺带两条：**取消不再清掉已填的内容**（误点一下不该把刚写的标题丢掉，建成之后才清），
  表单已展开时再按 Ctrl+N 会把光标送回第一格（以前毫无反应，看起来像坏了）。


- **换文章之后，那条待确认的「改地址」不再残留**。填了新地址还没确认就切到别的文章，

  确认按钮会把新地址落到**刚打开的这一篇**上（它取的是点确认那一刻的当前文章）。
  这个 bug 是刚补上的组件测试第一次跑就抓到的。


- **搬动内容时自动改写站内链接**（Dreamweaver 的 Update Links 那件事）。把一篇搬到
  别的栏目，站内其它文章里 `[见增量构建](/posts/incremental/)` 这类链接会跟着改到新地址；
  旧地址仍然照原样补进 `aliases`，外部链接靠重定向页兜着。以前只补 `aliases`，
  站内引用从此再也不跟着源头动，攒上几次搬动之后就没人说得清哪条还对。
  干跑里会单独标黄「另会改哪几篇、共几处」——这一步写的是你没有勾选的文件。
  改写规则：只动正文（front matter 里的 `aliases` 记的正是旧地址，不能碰）、
  围栏代码块里的地址不动（那是教学材料）、作者写 `/posts/a` 就还他 `/posts/a`
  而不顺手加尾斜杠、`#锚点` 与 `?查询` 原样保留、`/posts/ab/` 这类更长的路径不误伤。
  命令行 `batch move` 与 MCP `move_content` 同样会报出改了哪几篇。
  被改写的那一篇如果正开在编辑器里，会自动读回磁盘内容（有未保存改动时只提示，不覆盖）。


- **站内链接不用手打了**。`Ctrl+Shift+K`（也在「插入」菜单与工具条）挑一篇内容，
  地址由站点数据给出，插进去的链接一定存在。有选中的文字就拿它当链接文字。
  以前只有 `Ctrl+K`，插出来是 `[文字](https://)`——链到站内得自己记地址或者切去列表
  看一眼再切回来，手打的地址正是死链的来源，而死链要等「体检」页才发现。
  候选按标题、地址、源路径搜；草稿会标出来（草稿不进产物，链过去是死链），
  但不禁用——「先写互链、后补那一篇」是常见的写作顺序。
  HTML 正文格式的站点插的是 `<a href>` 而不是 Markdown 语法。


- **编辑长文像编辑一本书：大纲与字数**。编辑器多了大纲浮层（`Ctrl+Shift+O`、
  「编辑 → 大纲…」、工具条右侧「大纲」按钮三处入口），列出这一篇的标题结构，
  点一条或按回车把光标送到那一节；上下键移动。标题直接从源文抽，不经过渲染，
  所以边写边有，代码块里的 `# 注释` 也不会混进来。页脚同时给出字数与预计阅读时长
  （中文按字、西文按词，front matter 与代码块不算，按每分钟 300 字估算）。
  以前一篇上万字的文章只有一个滚动条：既跳不到「第三节」，也不知道自己写了多长。
  顺带修掉一处隐患——页脚原来那份字数用的是另一套规则（把链接地址也算进字数），
  现在与阅读时长共用同一份实现。


- **消息中心：通知飘走之后还能翻回来**。状态栏右侧多了「消息」（也在「视图」菜单与
  命令面板里），留最近 100 条通知，带时间与程度，可清空。通知气泡本来几秒就消失，
  而最需要事后再读一遍的恰恰是错误——手改 `staticsmith.toml` 存盘失败时，Rust 侧那条
  带行列号的 TOML 报错飘走之后就无处可查，用户只知道「保存没成功」。
  未读数只在有新消息时出现，打开即归零。
- **通知的抢读策略按程度分**：错误用 `role="alert"`（会打断读屏器当前朗读，操作失败要
  立刻知道），成功与提示用 `role="status"`。状态栏的「处理中…／进度」也进了无障碍通道
  （polite——它连续刷新，抢读会一直打断用户）。以前整块统一 polite，
  最该被听到的错误反而排在最后。

- **命令行也有安全网了**。`batch tags/draft/move/delete --yes`、`replace --yes`、
  `import`、`theme import` 在动手之前会留一份内容快照，并新增
  `staticsmith history list` 与 `staticsmith history restore <id> --yes`。
  桌面端与 MCP 一直这么做，命令行以前一次都不留——而命令行恰恰是最容易一次改上百篇
  的地方，脚本跑完源文件就没了。三方共用同一份历史（`.staticsmith/history.git`），
  在界面里也能看到脚本留下的那些。干跑、`build`、`deploy` 不留快照。

- **FTP 发布可以选「远端已存在同名文件时」怎么办**，选项照 FileZilla 那一栏：
  大小不同或本地更新（默认）/ 总是覆盖 / 仅本地更新 / 仅大小不同 / 已存在就跳过。
  在「设置 → 发布」选，「发布」页的发布目标里也显示当前规则。
  起因是默认判定有两种静默失败：服务器不支持 `MDTM` 时同长度的改动传不上去；
  服务器时钟快于本地时**所有文件被永久跳过**，报告只写「跳过 N 个」，
  看起来跟「没有改动」一样，而线上一直是旧的。这两种情况分不清「远端确实更新」
  和「时间戳在骗人」，只有你知道自己那台服务器什么样——撞上了就改成「总是覆盖」。
  选「总是覆盖」时不再逐个查远端状态，省下 N 次往返。
  远端没有的文件一律上传，与规则无关：「跳过」说的是不覆盖已有的，不是不发新文件。
  Git 发布不受影响，它比对的是内容哈希。

- **写操作前自动留本地快照，可一键回退**。批量替换、批量搬动、删除内容一直都有干跑
  预览，但落盘之后没有撤销——而 MCP 把这些写工具开给了 Agent，干跑只在人真的逐条读了
  预览时才起作用。现在 MCP 的写工具在动手之前会自动存一份快照，提交信息就是工具名；
  `list_snapshots` 看历史，`restore_snapshot` 退回去。回退**前**的状态也会先存一份，
  所以回退错了还能再回来。
  快照存在 `.staticsmith/history.git`，是一个独立于你自己 git 仓库的影子仓库：
  工作树是项目根，但仓库目录在 `.staticsmith` 里，项目根**不会**出现 `.git`，
  你的 `git log`、暂存区、`git status` 都不受影响。跟踪范围按配置里的实际目录算，
  只含源（配置、内容、模板、当前主题、静态资源），产物目录不进快照。
  你自己的 `.gitignore` / `.gitattributes` 不会影响快照内容，回退是字节级还原。
  改过目录名（比如把 `content_dir` 从 `./docs` 换成 `./content`）之后，回退到改名之前的
  快照也能把旧布局连内容一起还原——签出范围是「现在这套目录」与「那份快照里那套目录」的并集。

  只生成产物的操作与干跑不留快照。
  桌面端共用同一条历史：界面上的删除、批量动作、栏目改名与删除、跨文件替换、内容导入、
  主题装入、配置保存（表单与源码两处）、删除媒体在动手前同样会留快照。
  单篇保存、保存模板、新建文章 / 栏目、改栏目信息**故意不留**——它们改的是编辑器里
  正打开的那一份，撤销靠编辑器自己的缓冲；每次 Ctrl+S 都留一份只会把历史冲成噪声。
  界面上的入口在「编辑 → 回退内容…」：列出的是同一批快照（包括 Agent 留下的那些），
  选一条会先说明这次回退会做什么，确认后才动手。回退只改源文件，产物 `dist/` 不动，
  所以之后要重新生成一次。





- **新建站点可选版式**：起始页与 `staticsmith init --preset docs|blog` 都能选
  「橙色极简文档站」或「博客园风格博客」。菜单「站点 → 新建站点…」会弹一层表单，
  字段与起始页那张卡片一模一样（同一个组件，加字段只改一处）——版式推导不出来，
  事后换代价也不小（一套是十来个模板文件），所以两个入口都让你先选。
  两套写出的文件路径完全一致，只换模板与样式，所以换版式不影响任何既有的内容路径。



- **新建内容可以直接写路径**：`posts/2026/09/hello.md` 这种归档式目录推导不出来，
  只能手写。填了路径就以它为准，栏目输入框随之禁用——两个都参与推导会出现
  「栏目填 posts、路径填 notes/x.md」这种自相矛盾的输入。省略后缀会自动补 `.md`。

- **模板上下文补三个全站变量**：`terms`（词条及篇数，以前只在标签页有，所以全局侧栏
  做不出标签云）、`terms_by_field`（多维度站点按字段名取）、`feed_url` / `sitemap_url`
  （以前模板里只能手写 `/feed.xml`，与生成侧各写一遍迟早对不上；开关关掉或
  `base_url` 为空时是 `null`）。博客版式的侧栏据此换成了真标签云。

- **新建栏目可以同时填简介**：以前建完立刻就会被 SEO 体检记一条「缺描述」，
  等于每次建栏目都先欠一笔账。


- **站点可选正文格式**：「设置 → 构建 → 正文格式」在 Markdown 与 HTML 之间选，
  对应 `[build] source_format`。选 HTML 就是**原样输出**：写什么标签就是什么标签，
  `**` 只是两个星号。全站统一、不支持每篇覆盖，也不做「HTML 里仍跑一遍 Markdown」的
  混合模式（那种模式下「这段为什么被转义了」永远解释不清）。`content/` 下的
  `.html` / `.htm` 文件两种模式都会被收进来，切换格式不必改文件名。
  编辑器的高亮也跟着格式走：HTML 站点按标签、属性、注释着色，不再把满屏标签当普通文字。

- **设置页新增「源码」分段**：直接编辑 `staticsmith.toml`，逐字写回——不格式化、
  不重排键。表单认不出的键、写在配置里的注释、给别的工具看的段，现在都有地方改。
  写盘前先解析并校验，解析不了就不落盘（否则下一次打开站点连设置页都进不去）。
  它有自己的保存按钮：公共的「保存设置」写的是表单折算出的配置，会盖掉手写文本。

- **界面模式**：「视图」菜单里一键切换整套布局——标准、专注写作（只留编辑器与工具条）、
  源码（列表 + 编辑器，front matter 直接在 `+++` 里改）。类比 VS Code 的布局预设，
  记在本机，当前模式显示在状态栏右侧。模式只改布局不改能力：菜单、快捷键、
  命令面板在任何模式下都是全的；切过去之后每个窗格仍可单独开关。
  「视图」菜单同时多了「属性面板」「格式工具条」两个开关。
  定位也随之写明：正文就是源码，不做块级富文本（README 与 docs/ui.md）。

- **单篇内查找替换**：编辑器里 `Ctrl+F` 展开查找／替换那一行（上一处、下一处、
  替换这处、全部替换、忽略大小写，带「第 3 / 12 处」计数）。找**文章**改用
  `Ctrl+Shift+F`——`Ctrl+F` 现在总是「在你正看着的东西里找」。
  替换只改编辑器里的内容，落盘仍走 `Ctrl+S`。

- **跨文件查找替换**：「编辑 → 跨文件替换…」在侧栏展开，或命令行
  `staticsmith replace --find 旧名 --to 新名`，AI 助手侧是 `replace_text` 工具。
  改一个称呼、统一一个术语不必再逐篇点开。
  **只改正文，front matter 一个字节都不碰**（要改标题、标签用批量动作或属性面板），
  纯文本不支持正则，且**必须先看干跑结果**——哪几篇、共几处、每处前后对照，
  正文替换没有撤销栈。命令行默认只干跑（加 `--yes` 落盘），MCP 默认 `dry_run`。

- **侧栏能搜正文**：搜索框给两档结果——「标题或路径里有这个词」（本地即时过滤）与
  「正文里提到」（带一段上下文片段）。判断与 MCP 的 `search_content` 共用一份实现，
  界面搜到的与 AI 助手搜到的是同一批。
- **设置页新增「AI 接入」**：在界面里就能开 MCP 端点（只监听 127.0.0.1），
  不必再开终端跑 `staticsmith mcp`。权限默认只读，「改内容与模板」「执行发布」
  要显式勾选；权限只在启动时决定，界面显示的权限从正在跑的服务端读回来。
- **编辑区空态给出可点的三步**：写第一篇 → 生成一次 → 发布出去，完成态取真实状态，
  写过且生成过之后自动消失。
- **右键菜单**：内容条目与栏目分组头都能右键，与行内的「⋯」按钮共用同一份动作
  （打开、预览、发布/设为草稿、复制路径、删除；栏目还有新建、信息、改名、删除空栏目）。
- **日历能改排期**：「接下来」每行有「改期…」，「没写日期」每行有「补日期…」，
  就地改完即写盘。这一页从「看节奏」变成「排节奏」。
- **`staticsmith batch`**：加去标签、切草稿、搬栏目、删除四件事补进命令行，
  脚本化整理站点不必再回界面。搬动默认补旧地址，删除默认只干跑（真删要 `--yes`），
  判断与桌面端、MCP 共用 `staticsmith_core::batch`。
- 新增面向写作者的 [上手教程](docs/getting-started.md)：不假设读者懂 Markdown、
  Git 或命令行，走完一遍就有一个能发出去的站点。

### 变化

- **没有改动的重新生成快了 20 倍**：400 篇站点从 377 ms 降到 17 ms。上一条分阶段耗时
  查出九成五的时间在重算标签页，于是给它加了一道输入指纹（模板整体哈希 + 生效的维度定义
  + 分页大小 + 站点地址 + 全站每一页的内容哈希），指纹相同且产物还在盘上就整段跳过。
  受益的是 `serve` 下没有改动的重建、连点两次「生成」、CI 里的重复构建。
  指纹里的配置那一半是**整份全站上下文**（`site`、`build`、`menu`、`sections`、`terms`
  等模板能看到的全部），不是手挑字段——挑字段的第一版漏了 `site.title`：改了站名之后
  标签页不重算，页头上还是旧名字而页面看着完全正常。
  **改了内容仍然整体重算**（400 篇约 320 ms）：标签页 extend 的布局里有侧栏，
  两个自带版式的侧栏都用全局 `pages` 列最近文章，改任意一篇都可能改变每个标签页的输出。
  按词条粒度跳过会快得多，但会让侧栏停在旧数据上——页面看着正常，这种错最难发现，
  所以没做。理由与代价都写在 docs/architecture.md。
- **每次生成都告诉你时间花在哪**：`BuildReport` 带上分阶段耗时（计划 / 渲染 / 写盘 /
  站点文件 / 标签页 / 静态资源 / 索引），CLI 打在生成结果第二行，界面显示在「上次生成」
  下面（只列非零项，否则一屏 `0 ms` 反而看不见大头）。
  第一个发现就来自它：400 篇站点的**增量空跑 377 ms 里有 359 ms 在重算标签页**，
  渲染是 0——标签页每次整体重算是增量构建的主要成本。下一步给它也加增量判定。
- **构建性能有实测数字了**，不再只是「设计目标」。`cargo bench -p staticsmith-core`
  跑得到：全量 100 篇约 140 ms、400 篇约 1.12 s、增量改一篇（400 篇站点）约 450 ms、
  增量空跑约 260 ms。数字与解读写进了 docs/architecture.md「性能」一节，
  包括一个**被测量否掉的猜测**：把「每页序列化一次整站」改成「整站序列化一次、
  每页克隆一份」之后，全量构建没有明显变化（−2%，p > 0.05）——瓶颈是「每页都要一份
  整站数据的副本」，不是序列化那一步。真正去掉它要动模板契约，现在刻意不做。
- 内存预览现在**能看到图片与样式**了：本地样式表、图片、字体（含 CSS 里的 `url()`）
  会嵌进预览 HTML——`srcdoc` 沙箱没有文件访问权限，只能这么办。单个超过 2 MB
  或一页累计超过 8 MB 的会被放弃，预览栏底部写明还有几处没嵌进来。
  脚本与外链资源仍不加载，「与线上完全一致」仍然只有本地服务器模式能保证。
- 从未生成过时启动本地预览服务器，会先自动生成一次。服务器读 `dist/`，那目录还空着
  的时候点「启动」只换来白屏，用户会以为服务器起不来。只在从未生成过时补这一步——
  有旧产物至少能看，而每次启动都强制重建只会让人等。

- `Ctrl+S` 与 `Ctrl+F` 提升为全局快捷键：焦点在属性面板、侧栏、预览栏里也能保存；
  `Ctrl+F` 会自己切到「内容」页、必要时打开被关掉的列表栏再聚焦。
- `Ctrl+S` **按上下文分派**：外观页看着模板时保存模板，其余保存文章；菜单项标题
  跟着变（「保存当前模板」／「保存当前文章」）。补上了文档早就承诺、代码却没实现的
  「模板保存快捷键」。
- `Ctrl+P` 从开关改成「打开并聚焦」，与标识行按钮、视图菜单三处语义一致，关闭统一用 `Esc`。
- 生成警告合并成一条通知，不再逐条把通知区刷满一屏。
- 「重新读取磁盘上的内容与模板」成功后有反馈了（以前点了什么都不显示）。
- 窗口宽度不足时自动收起预览栏（< 1200px）与列表栏（< 900px）；手动动过的那一栏
  不再自动收放。窗口缩小时重新夹取分栏宽度，预览栏不会再把编辑器挤成一条缝。
- 内容筛选从五个药丸按钮改成一行下拉，选项文字把作用写全
  （「草稿（还没发布）」「待生成（改过还没生成）」这样）。
- 栏目列表页（`posts/index.md` 这类）在内容列表里带「栏目页」徽标，并在日历的
  「没写日期」区块说明它不参与排期——它此前跟文章长得一样，于是「日历上少了一篇」。
- 产物类型在两处叫同一个名字：「生成」页的产物清单与侧栏的「站点页面（生成）」
  统一说「标签页 / 分页页」。侧栏原先说「标签 / 分页」，还会把站点地图、订阅源
  一律标成「分页」。
- 主结构现在能只用键盘走：标签栏用 `←` `→` 换视图，分栏拖拽条能被 Tab 走到、
  方向键调宽（`Shift` 加速、`Home` / `End` 到两端），月历用方向键走格子、
  `PageUp` / `PageDown` 翻月、回车进入那天的条目、`Esc` 退回。
  一片结构只占一个 Tab 停靠点，不会为了跳过日历按四十多下 Tab。
- 按钮、输入框与分栏拖拽条的边界加深到能过 3:1（新增 `--border-strong` 令牌）。
  原来那档 `--border` 对底只有 1.45:1，而按钮底色与内容底只差 1.07:1——
  「这里能点」这件事此前几乎全靠形状猜。纯装饰的分隔线仍用原来的浅色，
  整套两套主题的令牌对比度核查结果写进了 docs/ui-design.md 6.11。
- 深色模式补齐令牌：主色与警告色有了深色值（砖红提亮且保持色相），
  新增成功色与「实心色块上的文字色」，界面里不再有绕过令牌的字面色值。

### 修复

- **符号链接不再是内容目录与主题目录的出口**。路径校验此前只在字符串上做，
  而 `content/外链` 指向别处时，`外链/x.md` 在**字面上**完全合法——`fs::write`
  会顺着链接写到目录外面去。现在字面校验之后再解析一次：从目标往上找到第一个已存在的
  祖先、规范化它、把剩下那截接回去比对（安全解压器的通行做法）。
  内容、栏目、主题包与媒体删除四处共用同一份判断，此前媒体那处单独实现了一份。
- **源目录走出项目根时会提醒你**，而不是只在日志里躺着。`content_dir` 等写成绝对路径
  或 `../` 是合法的，但有两个不显眼的后果：这个目录**不进内容快照**（安全网静默变小），
  路径校验也失去了「必须在项目根内」的参照。现在设置页顶部会显示，
  `staticsmith check` 以「注意：」打印。校验因此分成两层——阻断的（打不开项目）
  与提醒的（合法但有代价）；把提醒混进阻断会让一直正常工作的老项目升级后突然打不开。
  `output_dir` 指到站外不提醒：那是常规用法（等价于 `hugo -d /var/www`）。
- **不会再建出 `nul.md` 这种打不开的文件**。`nul`、`con`、`com1` 这类名字在 Windows 上
  是设备而不是文件：写入「成功」但内容进虚空、读回来是空的，还不报错。新建内容、
  新建栏目、装主题包现在一律拒绝这些名字，三个平台一致——在 Linux 上建出来、
  拿到 Windows 才发现打不开是最糟的顺序。只是「包含」保留名的普通名字不受影响
  （`console.md`、`com10.md` 照旧）。
- **预览服务器与文件监听不再因为一次崩溃就整个停掉**。这两个都跑在自己的后台线程上，
  循环体里一次 panic 以前会让线程结束：端口还在监听但什么都不回、外部改动再也不提示，
  而界面上仍写着「运行中」——没有报错，唯一线索是日志里那行 panic。现在循环体各自
  兜住 panic（`util::keep_running`），只让**这一轮**作废（一个请求不回、一批变更不通知），
  线程接着跑下一轮。
- **FTP 上传遇到网络抖动会重试，中断时说清传到哪儿了**。一次连接重置以前直接让整次
  发布失败，而那一刻线上已经是「一半新一半旧」，报告只说「上传 X 失败」——用户既不知道
  已经推上去多少，也不知道重来一次安不安全。现在单个文件最多试 3 次（间隔 300 ms，
  重试过程写进进度条），三次都失败才中止，错误里带上「这一批 N 个里已成功 M 个」
  并说明重发是安全的：差异同步会跳过已经传上去的那些。
  不做回滚——把旧文件再传回去是又一轮同样不原子的覆盖，失败时线上状态更难判断。
- **一次程序缺陷不再让窗口或 AI 端点从此每个操作都失败**。桌面端的状态锁与自身写入表
  以前用 `lock().expect(...)`：只要有一次 panic 发生在持锁期间（`with_writing_session`
  里留快照那一步就在锁作用域内，文件监听线程更不在兜底范围内），锁被标记为污染，
  此后**每个命令都会在取锁那一行再炸**——项目还开着，但什么都做不了，只能重启并丢掉
  未保存的东西。现在污染时取出里面的值继续用并留警告：`Option<Session>` 的一致性本来
  就由「写入路径 panic 后重开项目」负责，自身写入表里只是一串带 TTL 的路径。
  MCP 那侧同理，以前会永久回「内部状态锁异常」，现在恢复并从磁盘重载。
- **MCP 工具里的 panic 变成一条 JSON-RPC 错误**，而不是掀掉 HTTP 工作线程或 stdio
  主循环。以前端点会静默停止服务，而界面上还写着「运行中」，Agent 那边只看到连接断了。
  兜住之后立刻从磁盘重载项目状态——panic 可能停在改了一半的地方。
- **装主题包有了解压上限**：单个文件 8 MB、整包 64 MB、2000 个条目，超了在解压前
  就中止，一个字节都不写。以前没有上限，十几 KB 的包能声明出几 GB 的解压体积
  （zip bomb），内存或磁盘先撑不住；预览（先扫后装的第一步）也一样会被拖死，
  所以两处都查。声明的大小可以撒谎，真正读的时候还有一道硬顶。
- **媒体体检不会因为配置里粘进一个全角空格就崩掉**。`assets.url_prefix` 以多字节的
  空白字符开头时（从网页复制粘贴很容易带进 NBSP 或全角空格），扫描会在字符中间切开
  panic，整页体检挂掉。现在按字符前进。
- **浮层真的圈住焦点了**。「新建站点」与「回退内容」都写着 `aria-modal="true"`，
  但 Tab 一路走出去会落到背后的菜单栏与编辑器上——属性说的和实际行为不一致，
  读屏用户被带到一个本该「不存在」的地方。现在 Tab 在浮层内回卷（`src/focus.ts`）。
  顺带把三层浮层的「点空白关闭」从 `pointerdown` 改成 `click`：按下即关会在
  从遮罩起手拖选文字时误关。
- **发布页不再把「查不出来」说成「没配置 / 未存储」**。读凭据条目名或查凭据管理器
  状态失败时，以前分别退回空串与 `false`，界面于是显示「（未配置）」和「未存储」，
  输入框和保存按钮一起变灰——用户面对一个不能操作、又没有任何原因的死局，
  而凭证可能其实好好地存着。现在两处失败都会弹出原因，界面分开显示「状态未知」。
- **后台在忙时关得掉浮层了**。「回退内容」对话框的「关闭」「换一份」和「新建站点」
  对话框的「取消」以前都跟着全局忙态变灰，于是开着「保存即生成」的人一保存就
  关不掉浮层。取消与关闭不改变任何东西，不该受忙态影响。
- **新写的文章不再从栏目列表页上消失**。增量构建以前只看单个页面自己（哈希、模板、
  产物是否存在），而栏目列表页列的是同栏目**别的**页面——于是新建一篇文章后跑增量，
  只有这篇文章自己被渲染，`posts/index.html` 还是旧的，列表里看不见它；删掉一篇或
  改成草稿，列表里那条也不会消失。现在同栏目有页面新增、重渲染或消失时，列表页一起
  重建。开着「保存即生成」和用 `staticsmith serve` 的人受影响最大。
  标签页与 sitemap/feed 本来就每次整体重算，不受此影响。
- **命令内部出错不再带走整个窗口**。release 构建以前设了 `panic = "abort"`，
  任何一处程序缺陷都会让窗口当场消失、未保存的编辑一起丢。现在改成在 IPC 边界上兜住：
  你会看到一条错误信息，编辑器还活着，磁盘上的内容没受影响。写入路径捕获后会立刻从
  磁盘重载状态——出错可能停在改了一半的地方，拿着半套内存状态继续生成产物比报错更糟。
- `site_info` 与日志里的路径不再出现 `...\站点\.\content` 这种夹着 `.` 的写法。

- **保存设置不再抹掉配置文件里的注释**。以前「设置」页每保存一次就把
  `staticsmith.toml` 整文件重新序列化，你写的 `# 这台机器上别开 minify` 之类的说明、
  以及 StaticSmith 不认识的段（给别的工具看的那些）全部消失。现在走保序改写，
  与 front matter 同一套办法。唯一例外是数组表（`[[taxonomies]]`、`[[menu]]`）整块替换，
  它们内部的注释仍会丢——已写进 docs/configuration.md。
- 发布页的「凭据条目」名改由 Rust 侧算出（新增 `deploy_account` 命令）。
  界面此前自己拼 `git:<remote>`，与 Rust 侧各写一份：改了规则就会「保存写进 A、
  查询读的是 B」，用户看到的是「明明保存过，界面说没有」。
  顺带核实并写清了一件事——凭据按**发布目标**分而不按站点分，两个站点推同一个远端
  共用一条凭据是刻意的；代价是同一目标存两套凭据时后保存的会覆盖前一条。
- 生成结束后状态栏回不到「就绪」（进度文字停在最后一条）。
- 忙态在一次操作里反复跳变，按钮闪烁、`disabled` 提前解除：改成计数器，
  复合动作（保存、生成、批量、打开站点）整段圈成一次忙态。
- 空列表看起来像坏了：外观页四个列表、构建历史、发布结果、「站点页面（生成）」
  各自给出「接下来做什么」，不再整块消失或只剩一个标题。
- 键盘与读屏用户的一批问题：命令面板补 `aria-modal` 与焦点收束、浮层与菜单关闭后
  归还焦点、菜单标题上能用方向键、就地删除确认默认聚焦「取消」、
  改名与「栏目信息」表单展开即聚焦输入框。
- 分栏宽度每次变化都同步写一次 localStorage，拖动手感发涩：改成 200ms 防抖。
- MCP 的 `search_content` 搜的是渲染后的 HTML（搜 `img` 会命中每一张带图的文章），
  片段还丢掉了原文大小写。现在先还原成纯文本再搜，片段保留原文。
- 对齐 Tauri 的 npm 包与 Rust crate 版本，修掉 `npm run tauri dev` 起不来的根因。

## 2.0.0

首个记录在案的版本，能力范围见 [README](README.md#能力现状)。要点：

- 桌面端（Tauri 2 + Vue 3）：内容编辑与实时预览、栏目与批量动作、发布节奏日历、
  外观与主题包、三种体检、生成与发布、命令面板。
- 生成引擎（`staticsmith-core`）：Markdown + TOML front matter、Tera 模板与依赖图驱动的
  增量构建、SQLite 索引、内容寻址的媒体资源、taxonomy 聚合、sitemap 与 Atom 订阅、
  定时发布、旧地址重定向。
- 命令行（`staticsmith`）：`init` / `new` / `build` / `plan` / `serve` / `deploy` /
  `check` / `audit` / `import` / `theme` / `mcp`，`audit --fail-on` 可当 CI 门禁。
- MCP 服务端：三级权限（默认只读），供 AI 助手读写站点。
- 发布：Git 与 FTP/SFTP 差异同步，凭据存操作系统凭据管理器。
